package dev.tonholo.svg_to_compose.playground.ui.icon.template

import androidx.compose.ui.graphics.Color

/**
 * Custom color constant demonstrating the template system's color mapping.
 *
 * When a generated icon contains `Color(0xFF121212)`, the template engine
 * replaces it with this named constant and auto-imports it.
 */
val BLACK = Color(0xFF121212)
