# svg-to-compose (Core Library)

CRITICAL: MUST read and follow `AGENTS.md` in this directory for module-specific architecture, key files, and conventions.

This is the core KMP library handling SVG/AVG parsing and Compose `ImageVector` code generation.

```bash
./gradlew :svg-to-compose:build
./gradlew :svg-to-compose:allTests
./gradlew :svg-to-compose:detektMetadataCommonMain
```
