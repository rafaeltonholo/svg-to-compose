# buildSrc — Convention Plugins

CRITICAL: MUST read and follow `AGENTS.md` in this directory for module-specific conventions.

Contains precompiled Gradle script plugins that centralize all build configuration.
Do not duplicate build logic in module `build.gradle.kts` files.
